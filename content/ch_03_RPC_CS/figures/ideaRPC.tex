
\documentclass[tikz]{standalone}


\input{../../templates/tikzFigureHeader}

\begin{document}
  \begin{msc}{Local procedure call}
  % example: +
    % \declinst{c}{}{Client} 
    % \declinst{cc}{}{Channel API}
    % \declinst{sc}{}{Channel API} 
    % \declinst{s}{}{Server} 
    % % 
    % \regionstart{suspension}{s}
    % \nextlevel
    % \mess{Pass request   to channel}{c}{cc}
    % \regionstart{activation}{c}
    % \msccomment[side=left]{Do something}{c}
    % \nextlevel
    % \msccomment[side=left]{Block for reply}{c}
    \declinst{a}{}{Caller in address space A}
    \declinst{b}{}{Callee in address space B}
    % \regionstart{suspension}{a}
    \msccomment[side=left]{Prepare parameters for transport}{a}
    \nextlevel 
    \mess{Transport parameters, procedure ID}{a}{b}
    \nextlevel 
    \msccomment[side=right]{Prepare parameters for use}{b}
    \nextlevel 
    \msccomment[side=right]{Process}{b}
    \nextlevel 
    \msccomment[side=right]{Prepare result for transport }{b}
    \nextlevel 
    \mess{Transport result}{b}{a}
    \nextlevel 
    \msccomment[side=left]{Retrieve result, prepare for use }{a}
  \end{msc}
\end{document}